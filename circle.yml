test:
  override:
    - CIRCLE_NODE_INDEX=$CIRCLE_NODE_INDEX bash -e scripts/circle/before-script.sh
    - npm test && npm run test:codecov
machine:
  node:
    version: 6
  environment:
    NODE_ENV: test
    DATABASE_USERNAME: ubuntu
dependencies:
  override:
    - bash -e scripts/circle/install.sh
